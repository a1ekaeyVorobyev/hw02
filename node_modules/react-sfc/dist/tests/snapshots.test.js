"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
// import path from "path";
const compiler_1 = require("../src/compiler");
fs_1.default.readdirSync("tests")
    .filter((p) => fs_1.default.lstatSync("tests/" + p).isDirectory())
    .filter(p => !p.startsWith('__snapshots__'))
    .forEach((p) => {
    // console.log({ p });
    it(`passes ${p} snapshot test`, () => {
        const src = fs_1.default.readFileSync("tests/" + p + "/src.react", "utf8");
        const output = compiler_1.Compiler({ code: src });
        expect(output).toMatchSnapshot();
    });
});
